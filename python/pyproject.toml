[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "solana-keychain"
version = "0.1.0"
description = "Unified SolanaSigner interface across key-management backends"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
authors = [{ name = "Solana Foundation" }]
keywords = ["solana", "signer", "keychain", "ed25519"]
dependencies = ["httpx>=0.27", "solders>=0.26,<1"]

[project.urls]
Repository = "https://github.com/solana-foundation/solana-keychain"

[project.optional-dependencies]
aws-kms = ["boto3>=1.35"]
cdp = ["base58>=2.1", "cryptography>=43", "pyjwt>=2.8"]
crossmint = ["base58>=2.1", "cryptography>=43"]
dfns = ["cryptography>=43"]
fireblocks = ["cryptography>=43", "pyjwt>=2.8"]
fordefi = ["cryptography>=43"]
gcp-kms = ["google-cloud-kms>=2.24"]
openfort = ["cryptography>=43", "pyjwt>=2.8"]
privy = ["cryptography>=43"]
turnkey = ["cryptography>=43"]
utila = ["cryptography>=43", "pyjwt>=2.8"]
dev = [
    "base58>=2.1",
    "boto3>=1.35",
    "build>=1.2",
    "cryptography>=43",
    "google-cloud-kms>=2.24",
    "pyjwt>=2.8",
    "mypy>=1.14",
    "pytest>=8.3",
    "pytest-asyncio>=0.25",
    "respx>=0.22",
    "ruff>=0.8",
]

[tool.hatch.build.targets.wheel]
packages = ["src/solana_keychain"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["src", "."]
addopts = "-m 'not integration'"
markers = [
    "integration: live external-service tests, gated by environment variables",
]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["B", "E", "F", "I", "N", "UP", "W"]

[tool.mypy]
strict = true
files = ["src", "tests"]
mypy_path = "src"
explicit_package_bases = true

[[tool.mypy.overrides]]
module = ["boto3", "boto3.*", "botocore", "botocore.*", "google.*"]
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = ["tests.test_gcp_kms_signer"]
disallow_untyped_calls = false
